//! Generated by easymock

#ifndef EASYMOCK_{{file.name.upper().replace('.','_')}}
#define EASYMOCK_{{file.name.upper().replace('.','_')}}

#include <easymock.h>
#include "{{file.name}}"

{%for func in file.funcs:
{{Template("func.h.templ").render(locals()).out}}
%}

#endif
